/target
# Cargo home the integration containers write into the mounted checkout.
/.ci-cargo-home
.claude/worktrees/

# Local-only manual-testing fixtures generated by `scripts/vpn-lab.sh`
# (one ephemeral DigitalOcean droplet). Each `.ovpn` / `.conf` carries
# real droplet IPs + per-session credentials — never commit. Pattern
# unanchored so it matches `test-profiles/` at repo root and
# `scripts/test-profiles/` alike.
test-profiles/
