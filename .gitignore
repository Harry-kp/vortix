/target
.claude/worktrees/

# Local-only manual-testing fixtures generated by `scripts/test-infra.sh`
# (per-session DigitalOcean droplets). Each `.ovpn` / `.conf` carries
# real droplet IPs + per-session credentials — never commit. Pattern
# unanchored so it matches `test-profiles/` at repo root and
# `scripts/test-profiles/` alike.
test-profiles/
