# P0 — release gate

Every workflow here must pass on **macOS and Linux** before a release ships. They are
the checks that only a real kernel, a real terminal and real protocol daemons can
answer. Anything an automated test can decide is not in this file — see
[What this file deliberately does not cover](#what-this-file-deliberately-does-not-cover).

A workflow is written so an agent can execute it with no prior knowledge of Vortix:
every step is a literal command or keystroke, and every pass signal is something you
can see in captured output. If a step needs judgement, that is a bug in the step.

---

## Harness

The dashboard is a full-screen TUI, so drive it through `tmux` and read frames back
with `capture-pane`. This is the only supported way to run the TUI workflows.

```bash
VX=./target/release/vortix          # build first: cargo build --release

tui()  { tmux kill-session -t vx 2>/dev/null
         tmux new-session -d -s vx -x "${1:-120}" -y "${2:-40}"
         tmux send-keys -t vx "sudo $VX" Enter; sleep 6; }
key()  { tmux send-keys -t vx "$@"; sleep "${DELAY:-2}"; }
frame(){ tmux capture-pane -t vx -p; }
stop() { tmux send-keys -t vx q; sleep 2; tmux kill-session -t vx 2>/dev/null; }
```

- `tui 80 24` runs the 80×24 layout the dense workflows require.
- `key 'x'` sends a character, `key Enter` / `key Escape` / `key 'D'` send named keys.
- `frame | grep -n …` is how you assert. Capture **before and after** every keystroke
  that is supposed to change something; a pass that cannot be seen in a frame is not a pass.
- The TUI needs root. On macOS prime it once with `sudo -v` before starting; on a
  passwordless-sudo host nothing extra is needed.

**Keymap** (from the `?` overlay — do not guess these):

| Key | Action | Key | Action |
|---|---|---|---|
| `c` / `Enter` | connect / disconnect focused row | `i` | import profile |
| `c` (in-flight) | cancel connect | `R` | rename profile |
| `D` | disconnect **all** | `Del` | delete profile |
| `B` | connect both (route takeover) | `v` | view config |
| `K` | cycle kill switch | `a` | manage OpenVPN auth |
| `p` | switch theme | `A` | clear saved auth |
| `x` / `b` | action / bulk menu | `s` | cycle sort |
| `j`/`k` | move selection | `/` | search |
| `?` | help | `q` | quit |

---

## Prerequisites

| # | What | How |
|---|---|---|
| 1 | Release binary | `cargo build --release` on each host. The dev binary will not do — P0-33 tests the `opt-level="z"` profile specifically. |
| 2 | Real VPN endpoints | `scripts/vpn-lab.sh up` provisions one DigitalOcean droplet with all 15 named profiles. **It bills by the hour — run `scripts/vpn-lab.sh down` when the run is finished.** |
| 3 | Profiles imported | `vortix import <lab-profiles-dir>`. Workflows name profiles by their lab number (`wg07`, `03-openvpn-tcp-password-full`, …). |
| 4 | A second terminal | Several workflows need a concurrent client. `tmux new-window` is fine. |
| 5 | **Console access, if the host is remote** | Several workflows bring up a full tunnel (`AllowedIPs = 0.0.0.0/0`). On a machine you reach over SSH that captures the reply path and cuts your own session, with no way back in. Either run these on a host you can reach a console on, or keep every tunnel split-route. A `wg-quick` tunnel does not survive a reboot, so a power cycle is the recovery. |
| 6 | No competing VPN | A corporate/mesh VPN on the host owns routes and DNS and will make routing and leak workflows lie. Check with `ifconfig \| grep utun` (macOS) / `ip link` (Linux) and disconnect it first. |

Profiles used below: `wg07` split-route, `wg-full`/`wg10` full-tunnel, `wg11`
dual-stack, `wg14` DNS-negative, `03-…-password-full` OpenVPN password,
`04-…-static-challenge` OpenVPN OTP, `01-…-full-inline` OpenVPN cert-only.

---

## A. Startup and privilege

### P0-01 — Vortix refuses cleanly when it cannot own a terminal or root
Both platforms.
```bash
$VX                                  # no sudo
sudo $VX </dev/null >/tmp/o 2>/tmp/e ; echo "exit=$?"
sudo $VX | cat
ssh <this-host> sudo vortix
```
**Pass** — unprivileged run prints `Vortix needs administrator access to manage VPN
connections.` and `Try again with: sudo vortix`. Every non-TTY invocation exits 1 with
`The Vortix dashboard needs an interactive terminal` and names the headless commands.
**Fail** — a panic, exit 101, or a `ratatui`/`~/.cargo/registry` path anywhere in the output.

### P0-02 — A first run leaves nothing group-readable
Any platform. The assertion is that Vortix creates its directories private *regardless of
umask*, so set one rather than needing a 002-default distro: `( umask 002; vortix import … )`.
```bash
rm -rf ~/.config/vortix ~/.local/share/vortix
$VX import <profile>                 # unprivileged, before any sudo run
sudo $VX up wg07 && sudo $VX down wg07
ls -land ~/.config/vortix ~/.config/vortix/profiles ~/.local/share/vortix ~/.local/share/vortix/sessions
```
**Pass** — every directory is `drwx------`, and the import succeeded.
**Fail** — any `drwxrwxr-x`/`drwxr-xr-x` (a group member can swap a profile even though
the files are 0600), or `persisted control state is not a private owner-controlled file`.
Then repeat with `chmod 755 ~/.config/vortix ~/.config/vortix/profiles; $VX list` — an
upgrade must repair a directory an older release left world-readable.

### P0-03 — An unprivileged CLI still works after a sudo session
Both platforms, from no config directory.
```bash
rm -rf ~/.config/vortix
sudo $VX          # let the TUI draw, then q
$VX list; $VX status; $VX import <path>
ls -lan ~/.config/vortix/profiles ~/.config/vortix/lifecycle.lock
```
**Pass** — all three unprivileged commands work; every file is owned by your uid.
**Fail** — any uid 0 in that listing, a permission error, or `could not open its session lock`.

### P0-04 — A second instance refuses in one sentence
```bash
tui 80 24
$VX                                  # second process, same config dir
```
**Pass** — the second process exits immediately with one plain sentence saying Vortix is
already running. The first session is unaffected. With `b` open in the first session,
Bulk Actions lists only Import Profiles and Disconnect All, and the header does not say `Standard`.
**Fail** — an errno, a backtrace, a source location, or the first session disturbed.

### P0-05 — A missing VPN tool warns once and blocks nothing else
Linux.
```bash
sudo apt remove -y wireguard-tools
tui; frame | grep -i "wireguard-tools"
```
**Pass** — exactly one `'wireguard-tools' not found` warning naming the install command.
Listing, importing and OpenVPN profiles all still work; only `up` on a WireGuard profile
fails, with a dependency error.
**Fail** — repeating `Control service unavailable` toasts, or a missing tool blocking unrelated work.

### P0-06 — `vortix report` never blocks without a terminal
```bash
$VX report < /dev/null; echo "exit=$?"
$VX report | cat
ssh <this-host> vortix report
```
**Pass** — each prints the full report and exits 0 promptly.
**Fail** — any invocation hangs or loops on `Invalid choice`.

---

## B. Profile lifecycle

### P0-07 — Import accepts every supported shape and reports partial batches honestly
```bash
$VX import <lab-profiles-dir>        # mixed .conf/.ovpn, >8 files, one malformed
$VX import <single.ovpn> --json
$VX list
```
Then repeat the directory import from the TUI (`i`) while holding `j`/`k` to navigate.
**Pass** — every valid profile is admitted exactly once in filename order, the invalid one
is reported once, the exit code reflects partial success, and the terminal stays responsive
throughout. `--json` carries no raw config or credential material.
**Fail** — a valid profile silently dropped, the invalid one reported per-retry, a frozen
terminal, or a `profile updates failed` count on a later launch (P0-09 covers that).

### P0-08 — Malformed input is refused with a reason the user can act on
```bash
printf '' > /tmp/empty.conf
printf '[Interface]\nPrivateKey = x\n' > /tmp/nopeer.conf
printf '   \n\n' > /tmp/blank.conf
head -c 5000000 /dev/zero | tr '\0' 'x' > /tmp/huge.conf
cp <valid-wg> "/tmp/../../etc/passwd.conf" 2>/dev/null
for f in /tmp/empty.conf /tmp/nopeer.conf /tmp/blank.conf /tmp/huge.conf; do $VX import $f; done
```
**Pass** — each refusal names the rule it broke: `File is empty`, `Missing [Peer] section`,
`Missing [Interface] section`, `File too large … should be under 1 MB`. A filename that
cannot be a WireGuard interface names the naming rule.
**Fail** — any refusal whose only content is an operation id, or a generic `import failed`.

### P0-09 — Startup does not replay past imports as failures
On a config directory with real history
(`grep -c profile_mutation ~/.config/vortix/control/control-state.json` well above zero):
```bash
tui; frame | grep -Ei "profile update|updates failed"
```
Then import one good and one malformed profile.
**Pass** — startup shows no profile-update toast at all and no `Profile update failed`
lines in the Event Log; the live imports still report success once and failure once.
**Fail** — any `profile updates failed` count on launch, or a real import that stops being reported.

### P0-10 — Rename preserves identity; delete waits for a real disconnect
```bash
sudo $VX up 03-openvpn-tcp-password-full     # save credentials when prompted
```
In the TUI: focus the connected row, press `R` (refused while active), press `Del`
(refused while active). Disconnect, then rename it, restart Vortix, connect again.
**Pass** — rename is refused with a reason while the profile is active; after disconnect
the rename keeps the same stable id, the saved credentials still work, boot intent and
metadata still point at it, and no old-name files remain. `Del` becomes available only
once the profile reaches a terminal Disconnected state — never showing
`another operation already owns this profile` for a profile displayed as disconnected.
**Fail** — a re-prompt for credentials after rename, an orphan file, or a delete accepted
mid-`Disconnecting`.

### P0-11 — A stray metadata file explains itself instead of bricking the CLI
```bash
touch ~/.config/vortix/profiles/.stray.meta.toml
$VX list
```
**Pass** — the error names that exact path, explains that Vortix tracks profiles in an
inventory, and ends with a `mv` command that makes `vortix list` work again when run verbatim.
**Fail** — a blanket `Restore the managed profile directory to its saved inventory` with no
path and no command: every command including read-only ones is then locked out with nothing to act on.

---

## C. Connect and disconnect

### P0-12 — A cold one-shot CLI connects and disconnects on the first try
No TUI, no daemon, profile disconnected.
```bash
sudo $VX up wg07 --timeout 60; echo "exit=$?"
$VX status; $VX status --json | jq '.data.primary'
sudo $VX down wg07; echo "exit=$?"
```
Repeat for `01-openvpn-udp-full-inline`, and repeat each in `--json` and `--quiet`.
**Pass** — the first invocation converges instead of timing out. Human mode names the
transition, the target, the deadline and Ctrl-C behaviour *before* it waits, and ends on a
success line. `--json`/`--quiet` gain no progress text. Exit categories are stable.
**Fail** — a first-run timeout that a retry fixes (startup policy overwriting the first
lifecycle command), or progress prose polluting structured output.

### P0-13 — The TUI drives the same lifecycle and agrees with the CLI
```bash
tui 80 24
key j; key Enter                      # connect focused profile
frame | grep -iE "connected|connecting"
```
From a second terminal run `$VX status --json` while the TUI is open.
**Pass** — both surfaces report the same generation, primary, routes, health and kill-switch
state. The header, sidebar badge and Security Guard all move together. Disconnect from the
CLI and the TUI reflects it within about a tick.
**Fail** — the two surfaces disagreeing on primary or state, or the TUI needing a restart to catch up.

### P0-14 — Rejected OpenVPN credentials reopen the prompt next time
Real password profile (`03-openvpn-tcp-password-full`).
1. Connect, enter a **wrong** password, leave *save* enabled. Let it fail.
2. Connect again.
3. Connect with the correct password, disconnect, connect a third time.
4. Press `a` (manage auth).
**Pass** — attempt 1 fails with an authentication error and a toast saying the saved
credentials were removed; attempt 2 **reopens the prompt** rather than silently failing;
attempt 3 succeeds and attempt 4 reuses the stored pair with no prompt. `a` shows no stored
credentials after the rejection. A definitive rejection returns desired intent to
disconnected with no automatic retry and no surviving `openvpn` child.
**Fail** — the second connect failing without prompting, or a correct password not remembered.

### P0-15 — The OpenVPN static challenge is one-shot and memory-only
Profile `04-openvpn-tcp-static-challenge`, username/password already saved.
```bash
sudo $VX up 04-openvpn-tcp-static-challenge
```
Answer the OTP once. Then separately: try answering twice, Ctrl-C at the prompt, let the
deadline expire, and connect from a second client while a prompt is open.
```bash
sudo ls ~/.config/vortix/auth/ | grep -i scrv1        # must be empty
grep -riE "scrv1|<the OTP>" ~/.local/share/vortix/ ~/.config/vortix/
ps auxww | grep openvpn | grep -c "<the OTP>"
```
**Pass** — exactly one challenge is published per admitted operation, only the originating
client can answer, and it can answer once. No `.scrv1.auth` file is ever written. Cancel,
expiry and termination leave no secret file, socket, process or reconnect intent, and the
next attempt prompts again. No credential material in logs, snapshots, events or `ps`.
**Fail** — any OTP or password found on disk or in a process argument list.

### P0-16 — An unreachable peer times out, cleans up, and says so
Build the broken fixture from a **split-route** profile (`wg07`: `AllowedIPs =
10.200.0.0/24, 10.250.0.0/24`, no `DNS =` line). Never from a full tunnel: `wg-quick`
installs the default route and the resolver *before* the handshake is attempted, so a
connect that then fails can strand the machine with its egress and DNS pointing into a
tunnel that has nothing behind it. That is how this workflow took a laptop off the
network — the failure is the point of the test, so the fixture must not be able to carry
the whole machine with it.
```bash
sed 's/^Endpoint =.*/Endpoint = 192.0.2.99:51820/' wg07.conf > /tmp/wgdead.conf   # split route
$VX import /tmp/wgdead.conf
sudo $VX up wgdead --timeout 30
```
Do the same for an OpenVPN endpoint that starts but never reaches
`Initialization Sequence Completed`.
**Pass** — WireGuard stays `Handshaking` and never claims Connected or Protected; the
attempt ends at the configured timeout; desired state returns to disconnected; cleanup
removes the exact interface / OpenVPN process and route reservation **without reconnecting**;
the header and sidebar stop showing Connecting; an immediate retry is admitted.
**Fail** — a silent reconnect loop, a leftover interface or `openvpn` process, or a UI stuck in Connecting.

### P0-17 — Disconnect reaches terminal truth without a restart
```bash
sudo $VX up wg07; sudo $VX up 01-openvpn-udp-full-inline
sudo $VX down wg07                    # leave the other up
```
Repeat with the tunnel killed out from under Vortix first
(`sudo pkill openvpn` / `sudo ip link delete <iface>`), then disconnect normally.
**Pass** — the selected profile leaves `Disconnecting` as soon as protocol-owned absence is
proven, the other stays Connected, and closing/reopening the TUI is never required. Both the
clean and the already-gone path report a plain disconnect — no `DNS read-back is degraded`
and no `Disconnect could not finish safely`.
**Fail** — a profile stuck in `Disconnecting`, a stale presence restored by a late scan, or
`resolvectl status` still listing Vortix DNS on a dead link.

### P0-12a — Lifecycle verbs are idempotent and say so
Measure exit codes directly (`cmd >/dev/null 2>&1; echo $?`) — a pipe into `tail`
reports the pipe's status, not Vortix's.

| Command | Expected output | Exit |
|---|---|---|
| `up <profile>` twice | second prints `● Already connected to <profile>` | 0 |
| `down <profile>` twice | second prints `Already disconnected` | 0 |
| `down <never-connected>` | `Already disconnected` | 0 |
| `down` with no argument | `Disconnecting active VPNs …` then each profile | 0 |
| `up no-such-profile` | `Profile 'no-such-profile' not found` + `Run 'vortix list'` hint | 2 |
| `up <overlapping>` | names **both** profiles and the `--yes` bypass | 4 |
| `import <malformed>` | names the rule broken | 1 |

**Pass** — every row matches, and `down` with no argument leaves zero tunnel interfaces.
**Fail** — an exit code that does not distinguish not-found (2) from conflict (4) from a
general failure (1), or an idempotent repeat that errors.

### P0-12b — Two clients racing the same profile produce one tunnel
```bash
sudo $VX down wg08
( sudo $VX up wg08 --timeout 60 >/tmp/a1 2>&1 ) &
sleep 1
( sudo $VX up wg08 --timeout 60 >/tmp/a2 2>&1 ) &
wait; tail -1 /tmp/a1; tail -1 /tmp/a2
ip -brief link show | grep -c wg08        # Linux → 1
```
**Pass** — one client reports `● Connected`, the other `● Already connected`, both exit 0,
and exactly **one** interface exists.
**Fail** — two interfaces, a duplicate protocol child, or both clients claiming the connect.

### P0-12c — `reconnect` ends connected, or changes nothing
```bash
sudo $VX up wg08 --timeout 60
curl -4 -s --max-time 10 https://api.ipify.org     # the VPN exit
sudo $VX reconnect wg08 ; echo "exit=$?"
curl -4 -s --max-time 10 https://api.ipify.org     # must still be the VPN exit
$VX status --json | jq -c '{p:.data.primary,c:[.data.connections[].profile]}'
sudo $VX reconnect                                  # no argument: cycles every active tunnel
```
`reconnect` takes no `--timeout`; passing one is a parse error.
**Pass** — the profile is Connected afterwards, the egress address is still the VPN's, and
`primary` still names it. If the reconnect genuinely fails, the tunnel is either restored or
the message says the tunnel is now **down** — never "nothing was changed".
**Fail** — a message claiming nothing changed while the tunnel is gone and egress has
returned to the real ISP address. That is the worst outcome: the user believes they are still
protected.

### P0-12d — Every protocol variant in the matrix connects and tears down clean
Connect and disconnect each, one at a time, asserting the interface count returns to zero:
```bash
for p in wg09 wg10 wg12 wg13 wg15 01-openvpn-udp-full-inline 05-openvpn-udp-multi-remote; do
  sudo $VX up $p --timeout 60 2>&1 | tail -1
  sudo $VX down $p >/dev/null 2>&1
done
ip -brief link show | grep -cE 'wg|tun'   # → 0
```
Coverage: hostname endpoint (`wg09`), IPv6 endpoint (`wg10`), preshared key (`wg12`),
two peers with disjoint routes (`wg13`), explicit `FwMark` (`wg15`), OpenVPN inline
certificate (`01`), and OpenVPN whose first remote is unreachable so it must fail over (`05`).
**Pass** — each reports `● Connected`, `wg13` installs **both** peer routes
(`ip route show | grep -cE '10.200.0.0/24|10.201.0.0/24'` → 2), `05` reaches the working
remote after the dead one, and the final interface count is zero.
**Fail** — any variant failing to connect, or any leftover interface after the loop.

### P0-12e — Rapid and interrupted lifecycle actions leave no debris
Each case starts from disconnected and ends with the interface count back at zero.
```bash
count() { ip -brief link show | grep -cE 'wg|tun'; }   # macOS: ifconfig | grep -c '^utun[4-9]'
```

`1`-`9` quick-connect the profile at that index; `d` disconnects the focused row; `Shift+D`
disconnects all (confirming only when two or more are active).

| # | Action | Expected |
|---|---|---|
| a | `up wg08 &`, then `down wg08` ~2 s later | both report their own outcome; ends disconnected, count 0 |
| b | `up wg08 &`, then `up wg09 --yes` ~2 s later | both connect; count 2; each owns its declared routes |
| c | `up`/`down` the same profile 5× back to back | ends disconnected, count 0, no stuck `Connecting` |
| d | `up wg08`, `down wg08`, `up wg09` with no settle | wg09 connected, count 1 |
| e | SIGINT the client mid-connect | see below |
| f | TUI: `Enter` then `c` while the row shows `◐` | row returns to disconnected, count 0 |
| g | TUI: `7`,`8`,`9` 0.2 s apart | **all three** connect; count 3 |
| h | TUI: after (g), `Shift+D` then `Y` | count 0 and **stays** 0 — no tunnel appears afterwards |
| i | TUI: `Shift+D` with nothing connected | no dialog, stays disconnected |
| j | TUI: `d` on a row that is not connected | nothing disconnects |

**Pass** — every row lands on its expected state, the interface count returns to zero at the
end of each case, and no `openvpn` process survives (`pgrep -c openvpn` → 0). A second
profile connecting while the first is in flight must not cancel or adopt the first.
**Fail** — a stuck `Connecting`, an interface left behind, or two clients both claiming the
same tunnel.

**Case (e) is the sharp one.** There is no SIGINT handler in the CLI, so Ctrl-C stops *this
process waiting* — the admitted operation continues and can still bring the tunnel up. What
must be true afterwards is that Vortix still knows about it:
```bash
sudo $VX up wg08 --timeout 60 &   # then SIGINT the process group ~5 s in
ip -brief link show | grep -c wg08        # may legitimately be 1
$VX status                                 # must NOT say Disconnected while that is 1
curl -4 -s --max-time 8 https://api.ipify.org
```
**Fail** — `status` reporting `Disconnected` and `primary: null` while the interface exists
and `curl` returns the VPN's exit address. That is traffic flowing through a tunnel the
product says is not there, and the user has no way to know they are on it.

---

## D. Multi-tunnel and routing

### P0-18 — Two tunnels own the routes they claim
```bash
sudo $VX up wg-full                   # 0.0.0.0/0
sudo $VX up wg07                      # split, e.g. 10.8.0.0/24
ip route get 1.1.1.1 ; ip route get 10.8.0.1        # Linux
route -n get 1.1.1.1 ; route -n get 10.8.0.1        # macOS
curl -s --max-time 10 https://api.ipify.org
sudo iptables-save | grep -E "wg-full|wg07"          # Linux
sudo pfctl -a com.apple/vortix.killswitch -sr        # macOS
```
**Pass** — the default route resolves to the full-tunnel device and the split destination to
the split device; the public IP is the full tunnel's exit; the firewall has a per-tunnel
ACCEPT/pass-out for each interface and carves the split tunnel's declared CIDR. The sidebar
marks exactly one primary (`*`) and it matches the kernel.
**Fail** — a split tunnel holding the default route, or the sidebar naming a primary the kernel disagrees with.

### P0-19 — A route conflict offers the takeover confirmation, never a dead end
With `wg-full` connected and owning the default route, connect a second full-tunnel profile —
once from the TUI directly, and once by claiming the route from a **second client**
(`$VX up <other>` in another terminal) so the conflict lands after the TUI's last snapshot.
**Pass** — the takeover confirmation opens naming both profiles, the destructive default, the
choices and the IPv6 prefix summary with its `+N more` count. `Y`/`B` takes the route over,
`N`/`Esc` leaves both tunnels untouched with no relaunch. A conflict that the service refuses
still surfaces the confirmation.
**Fail** — a toast saying the VPN overlaps an active route with no confirmation to act on.
Also check the OpenVPN case where the conflicting route exists only in the server's
`PUSH_REPLY`: the candidate must be compensated back to Disconnected with the first tunnel untouched.

### P0-20 — A CLI disconnect does not clobber the other tunnel's protection
```bash
# TUI open with wg-full + wg07 connected, kill switch vpn-only
sudo $VX down wg-full                 # from a second terminal
cat ~/.config/vortix/killswitch.toml
sudo nft list table inet vortix_killswitch      # Linux
```
**Pass** — the persisted state still lists `wg07`'s interface and server IPs, and the firewall
keeps its ACCEPT rule. The TUI converges without a restart.
**Fail** — `wg07` losing its allowance because a sibling disconnected.

---

## E. Kill switch

> **These workflows deliberately cut your egress.** `vpn-only` installs a terminal
> `block drop out quick all` / default-DROP OUTPUT and permits only the tunnel interface and
> the VPN endpoint. If the tunnel goes away while the policy is installed, **all traffic
> stops** — that is the feature working. Before starting, know the recovery by heart:
>
> ```bash
> sudo vortix release-killswitch          # takes no --yes; only -C/--config-dir
> ```
>
> Never put the recovery behind a flag you have not verified, and never rely on a shell
> `trap` for it: if the release command fails to parse, the trap still tears the tunnel down
> and you are left blocked. Run the release as an explicit, checked step and confirm the
> anchor is empty before moving on.

Vocabulary is fixed: `off`, `block-on-drop`, `vpn-only`. There are no aliases.

### P0-21 — `vpn-only` engages immediately and holds through a drop
```bash
sudo $VX up wg-full
sudo $VX killswitch vpn-only
sudo nft list table inet vortix_killswitch        # Linux
sudo pfctl -a com.apple/vortix.killswitch -sr     # macOS
sudo wg-quick down wg-full                        # drop it externally
sudo iptables -L OUTPUT -n | head -3              # still policy DROP
curl -m 5 https://example.com; echo "curl=$?"
```
**Pass** — the firewall read-back shows the Vortix-owned terminal block policy *while the VPN
is still up* (not `Armed` with no enforcement), egress is blocked during the gap between drop
and reconnect, and no ICMP escapes. `$VX killswitch` and the Security Guard both say
`VPN-only`, never a raw enum name.
**Fail** — `Armed`/`Watching` while `vpn-only` is selected, or any egress succeeding during the gap.

### P0-22 — `block-on-drop` reports the block it actually engaged
```bash
sudo $VX killswitch block-on-drop
sudo $VX up wg-full
sudo ip link delete wg-full           # or: sudo pkill openvpn
curl -m 5 https://example.com; echo "curl=$?"
sudo nft list table inet vortix_killswitch
```
**Pass** — egress is blocked, the table shows the barrier, the header reads `KS:DROPPED` and
Security Guard says `Killswitch: VPN dropped` with `press r to reconnect` — and it **holds for
several minutes**, not five seconds. With the VPN merely connected and no drop, the table does
not exist, `$VX killswitch` prints `Watching`, and the row stays `Block on drop` for five
minutes without flipping to `Degraded`.
**Fail** — `KS:DEGRADED` at any point while that table is present and correct, the row
oscillating between the two, or a `Degraded` row while the table is genuinely absent.
Then make a reconnect fail past the tunnel barrier: the pre-tunnel barrier must **stay
installed** (fail-closed) and keep reading `KS:DROPPED` over it — never torn down by failure
compensation, never installed-but-unreported.

### P0-23 — Cycling the kill switch quickly stays truthful
```bash
tui; key j; key Enter; sleep 20       # get a tunnel up
DELAY=0.3; for i in 1 2 3 4 5; do key 'K'; done; DELAY=2; sleep 10; frame | grep -i killswitch
```
**Pass** — the header target cycles immediately on every press, exactly one change is applied
per settle, Security Guard never shows `Degraded` while a change is in flight, and no
`kill switch change timed out` appears.
**Fail** — any timeout, or `Degraded` shown while the nft table is present and correct.

### P0-24 — Emergency release removes Vortix state and only Vortix state
With `vpn-only` engaged, two tunnels up, and an **unrelated** VPN or firewall rule also present:
```bash
sudo $VX release-killswitch
sudo nft list ruleset | grep -c vortix_killswitch     # Linux → 0
sudo pfctl -a com.apple/vortix.killswitch -sr         # macOS → empty
```
Repeat after a forced failure: a malformed kill-switch state file, a corrupted control
journal, and a normal startup failure. On Linux also seed duplicate legacy Vortix-owned
IPv4/IPv6 OUTPUT jumps from an older release first.
**Pass** — release works even when normal startup is broken, removes every Vortix-owned rule
including legacy ones, proves absence, leaves the unrelated policy and the external VPN
session byte-identical, and durably fences unreadable control history to `off`.
**Fail** — an unrelated rule removed, an external session disturbed, or protection still claimed after release.

---

## F. DNS

### P0-25 — WireGuard DNS is applied by Vortix, never by `wg-quick`, and reverted exactly
Profile with `DNS = 1.1.1.1` (`wg-full`).
```bash
resolvectl status                                  # Linux: capture BEFORE
scutil --dns | head -20                            # macOS: capture BEFORE
sudo $VX up wg-full
resolvectl status wg-full                          # DNS Servers: 1.1.1.1, Default Route: yes
cat ~/.config/vortix/dns-policy.state
sudo $VX down wg-full
```
Also confirm the short-lived config handed to `wg-quick` has no `DNS=` line, and that the
source profile is byte-identical afterwards. Repeat the disconnect after restarting Vortix
while the interface stays up, and with a forced disconnect.
**Pass** — the resolver is set on the tunnel link, `dns-policy.state` records the parsed
request, every `down` uses a DNS-free managed config, the source profile never changes, and
disconnect restores the exact prior resolver. An unrelated link is byte-identical throughout.
On a resolved-native host with **no** `systemd-resolvconf`/`openresolv` installed the
dep-check must not raise `Missing dependencies: resolvconf (systemd)`.
**Fail** — `wg-quick` mutating the resolver, a replayed profile DNS on teardown, or a foreign
link modified.

### P0-26 — OpenVPN pushed DNS reaches the policy, not the protocol
Full-tunnel OpenVPN profile whose DNS exists only in `PUSH_REPLY`.
```bash
sudo $VX up 01-openvpn-udp-full-inline
ps auxww | grep openvpn | grep -o "pull-filter[^ ]* [^ ]* [^ ]*"
cat ~/.config/vortix/dns-policy.state
dig +short example.com
sudo $VX down 01-openvpn-udp-full-inline
```
**Pass** — the OpenVPN child carries Vortix's pull filters for `dhcp-option DNS` and
`dhcp-option DOMAIN`, the pushed resolver appears in `dns-policy.state` for the current
tunnel, hostname traffic resolves through the VPN, and disconnect restores the prior resolver.
**Fail** — any protocol-side DNS mutation, or a resolver that survives disconnect.

### P0-27 — A DNS failure degrades the truth instead of faking protection
Linux-only as written: it breaks `resolvectl`. macOS reads DNS through the
SystemConfiguration API — that code *replaced* `scutil --dns`, so there is no binary to
disable and no equivalent handle for this fault. Read the verdict from the **Security
Guard panel**, not `vortix status`; the CLI has no DNS row and looking there reports a
false gap.
```bash
sudo $VX up wg-full
sudo chmod -x $(command -v resolvectl)             # Linux; break read-back only
sleep 10; frame | grep -iE "dns|unverified"
RUST_LOG=vortix::control::policy=warn $VX status
$VX report | grep -A3 protection_gates_unverified
sudo chmod +x $(command -v resolvectl)
```
**Pass** — the tunnel stays Connected and routes packets, Security Guard shows
`DNS: Unverified` and the headline demotes to partial, the `Killswitch` row stays on its own
mode, the log names `gate="dns"` with the platform reason, `vortix report` carries
`protection_gates_unverified` with `dns: false`, and everything recovers within five seconds
of restoring the binary.
**Fail** — the kill-switch row reporting the DNS failure, any surface claiming verified DNS
protection, or nothing naming the gate.

### P0-28 — A competing VPN's DNS route fails safely with actionable guidance
Keep Tailscale/NetBird (or any VPN) connected with a more-specific route to the resolver a
Vortix profile requests, then connect that profile.
**Pass** — Vortix restores the previous network settings, the TUI explains that another VPN or
network service is routing the profile's DNS traffic and asks the user to disconnect it, and
the Event Log names the resolver, the observed and expected interfaces, and the matching
`route -n get` / `ip route get` command. After stopping the competing VPN the same profile connects.
**Fail** — a claim of protection over a resolver route that leaves the tunnel.

---

## G. Security Guard truth

### P0-29 — Real IP and Exit IP survive a launch-with-VPN-up
```bash
rm -f ~/.config/vortix/real-ip.cache ~/.config/vortix/real-ipv6.cache
tui                                   # no VPN — wait for Real IP to populate
ls -l ~/.config/vortix/real-ip.cache  # mode 600, your ISP address
key j; key Enter; sleep 25            # connect a full tunnel
stop                                  # quit while still connected
tui                                   # relaunch with the kernel tunnel still up
frame | grep -iE "real ip|exit ip"
```
**Pass** — with a cache present the `Real IP` row populates **immediately** from disk and shows
your ISP address, never the VPN's exit IP. With the cache deleted it reads `detecting…` /
`checking…` instead of borrowing the exit IP, and populates once you disconnect. A cache older
than a day is never shown as a confirmed current fact; under a day it renders greyed and
tagged `last known` with no success mark.
**Fail** — the VPN's exit IP presented as the real IP, or a stale address used to claim the exit is masked.

### P0-30 — IPv6 is reported honestly on a dual-stack host
Host must have working IPv6 (`curl -6 -s --max-time 5 https://ifconfig.co/ip` answers).
Record `REAL_V6` before connecting.
```bash
sudo $VX up wg11        ; curl -6 -s ifconfig.co/ip     # dual-stack tunnel
sudo $VX up wg-full     ; curl -6 -s ifconfig.co/ip     # v4-only tunnel
sudo $VX up wg07        # split-only
```
**Pass** — on the dual-stack tunnel the v6 address **changes** and `Exit IPv6` reads it with a
green `✓`. On the v4-only tunnel `curl -6` still returns `REAL_V6` and `Exit IPv6` reads it
with a red `✗` and `v6 exposed — matches real IPv6`, demoting the banner to PARTIAL. On the
split-only tunnel both `Exit IPv4` and `Exit IPv6` read `split-route — no exit` with `─` and
**no** alarm. Disconnect/reconnect flips the rows back within a tick with no stale hangover.
**Fail** — a v6 leak reported as protected, a split-only tunnel alarmed as leaking, or rows
that stick after a state change.

### P0-31 — Telemetry stays current and does not flood the Event Log
Connect a busy tunnel, open the Event Log at info level, leave it for at least two telemetry intervals.
```bash
sudo $VX up wg-full; sudo $VX killswitch vpn-only
tui; key f                            # cycle log level to info
```
Then block the telemetry endpoints for ten poll intervals and restore.
**Pass** — transfer, health, packet-loss and jitter rows keep updating and the footer age
stays under a minute; routine samples emit no per-sample lines while each category transition
emits one; identical successful probe results add one line, a change or a total failure adds a
new one. A standing outage produces exactly one line when it starts and one when it clears.
Every `Public IPv4 changed` line names a dotted-quad on both sides. On an IPv4-only full tunnel
with `vpn-only`, Exit IPv4, Location and DNS all still refresh and the firewall's IPv6 drop
counter does not climb in step with the polls.
**Fail** — a line per poll, a row settling on `unavailable`, a growing footer age, or an IPv6
address inside an IPv4 line.

### P0-32 — Security Guard fits 80×24 with nothing dropped
```bash
tui 80 24; key j; key Enter; sleep 25; frame
```
**Pass** — both section words (`Identity`, `Defense`), all seven content rows (Real IP, Exit IP,
Location, DNS, Killswitch, Encryption, IPv6) and the `Updated …` footer are present, with no row
dropped by `compact_to_fit` and no word truncated mid-way. When an alarm fires, **only** that row
goes bright/bold and gains a sub-line — the others stay muted.
**Fail** — a missing Location or IPv6 row at the bottom, or more than one row shouting at once.

---

## H. Terminal behaviour

### P0-33 — The size-optimised release build stays responsive
`cargo build --release` (`opt-level = "z"`), real terminal, 80×24, 15 profiles.
Hold `j`/`k` to scroll, flip a panel and watch its 25 ms animation, resize the window
repeatedly while connected, connect and disconnect two tunnels.
**Pass** — held-key scrolling tracks with no visible lag or dropped frames, the flip animation
runs smoothly to completion rather than snapping, a resize repaints within one tick, and
nothing feels slower than the same session on a dev build.
**Fail** — perceptible input lag, a stuttering or skipped animation, or a repaint trailing the resize.

### P0-34 — Logging and colour never paint over the TUI
```bash
for v in "" "RUST_LOG=" "RUST_LOG=off" "RUST_LOG=trace" "RUST_LOG=vortix::process=info" \
         "RUST_LOG=vortix=warn,mio=trace" "RUST_LOG=!!!bogus"; do
  env $v $VX list 2>/tmp/err; echo "$v -> $(wc -l </tmp/err) stderr lines"; done
NO_COLOR=1 sudo $VX                   # then read the badges
```
**Pass** — unset, empty, `off` and malformed all print zero stderr lines and leave the
alternate screen clean; `trace` prints every target; target-scoped forms print only their
target at or above the named level. Under `NO_COLOR=1` status badges stay distinguishable by
Unicode shape alone.
**Fail** — any log line painted over the TUI when the filter should be silent, or badges that
are only distinguishable by colour.

### P0-35 — Dense actions stay profile-scoped and legible at 80×24
```bash
tui 80 24                             # two profiles active
```
Focus an **inactive** row and press `d` (nothing else disconnects). Focus active and
transitional rows and confirm `c`/`d` and the footer agree on Connect, Cancel, Disconnect and
Force Kill. With the inactive row focused press `D`, confirm Disconnect All. Open `b`. Paste a
path longer than 58 columns into Import. Trigger an error, then open and cancel another overlay
with one `Esc`.
**Pass** — only the two active rows turn orange while disconnected rows stay neutral; every
tunnel disconnects with one `All VPN connections disconnected` toast, not a stale
`connection cancelled`. `b` contains only Import Profiles and Disconnect All. The long path
keeps its cursor visible. Routine info does not replace a visible error, a newer actionable
warning or terminal result does, and the error clears itself after about eight seconds without
needing `Esc`.
**Fail** — `d` on an inactive row disconnecting something, a stale toast, or an error that
needs a keystroke to clear.

### P0-36 — Every built-in theme is readable
Press `p` through Synthwave, Terminal, Catppuccin Mocha, Dracula, Nord, Gruvbox Dark and
Tokyo Night at 80×24, inspecting the dashboard and each overlay. Test Terminal against a
light and a dark terminal background. Restart and cycle back to Synthwave.
**Pass** — every frame changes immediately with one concise toast naming the theme; status,
selection, border, chart, toast and overlay colours stay readable and semantically distinct;
`config.toml` keeps its comments and unrelated settings and records only the selected theme;
the choice survives restart; Terminal inherits the terminal's own defaults.
**Fail** — an unreadable pairing in any theme, a lost comment in `config.toml`, or stale
colours after a cycle.

---

## I. Secrets on disk and in process state

### P0-37 — Credentials never reach a command line, a log, or a world-readable file
With an OpenVPN password profile connected and a WireGuard tunnel up:
```bash
ps auxww | grep -c "<the password>"                       # → 0
sudo ls -la ~/.config/vortix/auth/                         # 0600, owned by you not root
ls -la ${TMPDIR:-/tmp}/vortix-*/ 2>/dev/null               # 0600
grep -rniE "<password>|PrivateKey" ~/.local/share/vortix/
sudo $VX down <profile>; ls -d ${TMPDIR:-/tmp}/vortix-*/ 2>&1   # gone
```
Also: connect via `sudo $VX up`, and confirm the auth file is owned by the **invoking user**,
not root. Then replace it with a symlink to `/etc/shadow` and reconnect.
**Pass** — zero matches in `ps` and in the journal, auth and temp files are 0600 and owned by
the config owner, temp directories disappear on disconnect, and the symlinked auth write is
refused with `/etc/shadow` unchanged.
**Fail** — any credential in `ps` or on disk unprotected, a root-owned auth file after `sudo`,
or a followed symlink.

### P0-38 — A stale or unsafe saved-credential file is rejected, not used
```bash
printf 'user\nSCRV1:cA==:MTIzNDU2\n' | sudo tee ~/.config/vortix/auth/<profile>.auth
$VX list                              # any command triggers startup cleanup
stat ~/.config/vortix/auth/<profile>.auth      # must not exist
```
Repeat with a wrong-owner file, a mode-0644 file, an oversized file and a hard link.
**Pass** — the stale SCRV1 file is removed with a warn-level event naming the profile under
`RUST_LOG=vortix::utils=warn`, and the next connect re-prompts cleanly. Every unsafe fixture
is left untouched and re-prompts rather than being used.
**Fail** — an unsafe credential file consumed, or a stale one silently kept.

---

## Results log

Record one run per release, per platform. A workflow that was not run is `—`, never a blank.

**Run of 2026-09-16/17** (`8b655e1`, lab droplet `139.59.71.126`). `PASS*` = the workflow's own
assertions held but a defect outside its scope was observed; `?` = inconclusive, the fixture
could not distinguish pass from fail. Findings are listed under the table.

| # | Workflow | macOS | Linux |
|---|---|---|---|
| P0-01 | Refuses without terminal or root | PASS | PASS |
| P0-02 | First run leaves nothing group-readable | PASS | PASS |
| P0-03 | Unprivileged CLI after a sudo session | PASS | PASS |
| P0-04 | Second instance refuses in one sentence | PASS | PASS |
| P0-05 | Missing VPN tool warns once | n/a | PASS |
| P0-06 | `report` never blocks without a terminal | PASS | PASS |
| P0-07 | Import accepts every supported shape | PASS | PASS |
| P0-08 | Malformed input refused with a reason | PASS | PASS |
| P0-09 | Startup does not replay past imports | PASS | PASS |
| P0-10 | Rename preserves identity, delete waits | PASS | PASS |
| P0-11 | Stray metadata file explains itself | PASS | PASS |
| P0-12 | Cold one-shot CLI connects first try | PASS | PASS |
| P0-12a | Lifecycle verbs idempotent, exit codes distinct | PASS | PASS |
| P0-12b | Racing clients produce one tunnel | PASS | PASS |
| P0-12c | `reconnect` ends connected or changes nothing | PASS | PASS |
| P0-12d | Every protocol variant connects and cleans up | PASS | PASS |
| P0-13 | TUI lifecycle agrees with CLI | PASS | PASS |
| P0-14 | Rejected credentials reopen the prompt | PASS | PASS |
| P0-15 | Static challenge is one-shot, memory-only | PASS | PASS |
| P0-16 | Unreachable peer times out and cleans up | PARTIAL | PASS |
| P0-17 | Disconnect reaches terminal truth | PASS | PASS |
| P0-18 | Two tunnels own the routes they claim | PASS* | PASS* |
| P0-19 | Route conflict offers the confirmation | PASS | PASS |
| P0-20 | CLI disconnect spares the sibling | PASS | PASS |
| P0-21 | `vpn-only` engages and holds through a drop | PASS* | PASS* |
| P0-22 | `block-on-drop` reports its barrier | PASS | PASS |
| P0-23 | Rapid kill-switch cycling stays truthful | PASS | PASS |
| P0-24 | Release removes only Vortix state | PASS | PASS |
| P0-25 | WireGuard DNS applied and reverted exactly | PASS | PASS |
| P0-26 | OpenVPN pushed DNS reaches the policy | PASS | PASS |
| P0-27 | DNS failure degrades the truth | n/a | PASS |
| P0-28 | Competing VPN DNS route fails safely | blocked | — |
| P0-29 | Real/Exit IP survive launch-with-VPN-up | PASS | PASS |
| P0-30 | IPv6 reported honestly | n/a | PASS |
| P0-31 | Telemetry current without flooding | PASS | PASS |
| P0-32 | Security Guard fits 80×24 | PASS | PASS |
| P0-33 | Release build stays responsive | PARTIAL | — |
| P0-34 | Logging and colour never paint over the TUI | PARTIAL | PASS |
| P0-35 | Dense actions stay profile-scoped | — | PASS |
| P0-36 | Every built-in theme is readable | PASS | PASS |
| P0-37 | Credentials never reach argv, log or disk | PASS | PASS |
| P0-38 | Unsafe saved-credential file is rejected | PASS | PASS |

### Findings from the 2026-09-16 run

Ranked by severity. Two were fixed during the run; the rest are open.

| # | Finding | Platform | State |
|---|---|---|---|
| F13 | **A full dual-stack tunnel is reported as having no exit.** `wg11` is `AllowedIPs = 0.0.0.0/0, ::/0`. Connected, `curl -6` returns the droplet's `2400:6180:100:d0:0:1:8b7f:5001` (not the host's `2401:4900:…`) and the header reads `● CONNECTED (wg11/WG) │ VPN: 139.59.71.126`, so both families genuinely egress through it — yet Security Guard renders `Exit IPv4 : split-route — no exit ─` and `Exit IPv6 : split-route — no exit ─`, verdict PARTIAL. The v4-only case is correct after `8b655e1`; the dual-stack claim is still mis-derived. | Linux | fixed `3ce196c` |
| F15 | **A credential file refused for unsafe permissions reports the wrong reason.** `chmod 644` (or `640`) on a saved `.auth` correctly makes Vortix refuse to use it — but the error is `profile '03-openvpn-tcp-password-full' requires 2FA but stdin is not a tty`. Profile 03 has no 2FA; it is plain username/password. The actual reason, that the saved credential file is group- or world-readable, is never stated. | both | fixed `1d88c98` |
| F14 | **The auth overlay for a static-challenge profile opens focused on the wrong field** — the third box (the server's challenge text, e.g. `Vortix lab`) rather than Username, so typing immediately fills the wrong one. Shift+Tab twice is needed to reach the top. | both | fixed `1d88c98` |
| F19 | **Primary election is intermittent in the TUI on macOS.** With `wg08` (`AllowedIPs = 0.0.0.0/0`) connected, the kernel default route is its `utun4`, `wg show` confirms `0.0.0.0/0`, and `status --json` reports `primary: wg08` — yet the dashboard held `○ NO EXIT │ Real: 139.59.71.126` with `Exit IP : split-route — no exit` for 75 s. The same profile rendered correctly in other runs, so `8b655e1` improved this without closing it: `observed.default_route` is not reliably populated on macOS. Note the no-primary header branch also labels the exit address as `Real:`. | macOS | open |
| F18 | **An externally-owned tunnel poisoned the real-IP cache.** The gate required a ticked scanner, no managed session and a disconnected registry — all Vortix's own bookkeeping, all satisfied by a VPN it did not start while that VPN carried the egress. `real-ip.cache` was written with the VPN's exit address and Security Guard then showed Real IP equal to Exit IP with a false leak. Caching now also requires that the kernel's default route is not a tunnel device, and the EXPOSED panel no longer presents an unmanaged tunnel's exit as the real address. | macOS | fixed `6e491f3` |
| F17 | **Rapid quick-connects are lost, and a deferred one can outlive Disconnect All.** Pressing `7`,`8`,`9` 0.2 s apart connects only some: 2 of 3 on Linux, 1 of 3 on macOS (wg08 reached `HANDSHAKING` then vanished with nothing in the Event Log). On Linux the third then completed **after** a Disconnect All — `Shift+D` removed wg07 and wg08 and wg09 appeared, so a second Disconnect All was needed to reach zero. Asking to disconnect everything can leave a tunnel up. | both | open |
| F16 | **An interrupted connect leaves a live tunnel the product denies.** There is no SIGINT handler in the CLI, so Ctrl-C during `up` does not cancel — the operation completes and the interface comes up. Afterwards `vortix status` says `Disconnected` with `primary: null` while the interface exists and `curl` returns the VPN's exit address: traffic is flowing through a tunnel Vortix says is not there. The progress line promised "Ctrl-C cancels", which is now corrected, but the reporting gap remains. Reproduced on both platforms. | both | open |
| F20 | **Arming `block-on-drop` then connecting blocked all egress.** The pre-tunnel barrier was built from an empty tunnel list — its policy digest was `e3b0c442…b855`, the SHA-256 of the empty string — so it allowed only loopback, RFC1918 and DHCP before dropping everything, and the first thing it blocked was the WireGuard handshake it existed to cover. The connect failed and the barrier stayed up over a host with no route out. A barrier with nothing to allow is now refused. Verified: the connect succeeds, `nft=0`, egress on the VPN; `vpn-only` (13 rules) and armed `block-on-drop` (quiet) both unaffected. | Linux | fixed `aaad0af` |
| F12 | **`block-on-drop` did not engage on an unexpected drop.** The barrier was installed (`killswitch.engage` fires) and then removed by the same transition's final stage, which disabled blocking unconditionally for `Auto`. Gating it needs to know the tunnel came back, and the obvious signal lies: a drop opens its recovery in the pass that notices the loss, so the newest fact still reads `observed=true` moments after the interface is gone. Now carries the capture instant and, **on a recovery only**, counts a tunnel as back only from a fact at least that new. Verified both directions: arm-then-connect connects with `nft=0` and egress on the VPN; deleting the interface gives `nft=13` and `egress=[]` instead of the ISP address. | both | fixed `7833633` |
| F21 | **`block-on-drop` holds its barrier while healthy under a supervisor.** With the dashboard running, `nft=13` with a healthy tunnel — traffic still flows through it and a drop is still blocked, so the behaviour is strictly safer, but CLAUDE.md specifies the mode stays quiet while up and this is `vpn-only`'s shape. Not seen on the CLI-only path (`nft=0`). Surfaced by the F12 fix. | Linux | open |
| F9 | **`vortix reconnect` tears the tunnel down and leaves it down**, then reports *"nothing was changed"*. Trace: `wg-quick down …` succeeds, then `wg show wg08 dump` exits 1 — the interface it just removed — and that is treated as the failing step; the re-up never runs. The convergence trace shows the deadlock: `desired=Connected observed_active=true truth=Some(DisconnectedTombstone) tombstoned=true` → `tunnel_barrier_ready=false`, with `fact_age_millis` climbing because the failed probe never replaces the stale "active" fact. Afterwards: 0 links, egress on the real IP, `primary: null`. | Linux | fixed `565b056` |
| F8 | **`vpn-only` reports `Degraded` while its policy is installed and enforcing.** 9 PF rules incl. the terminal `block drop out quick all` (macOS) / nft table with `policy drop` (Linux), traffic correctly confined, sampled at t+2/8/20/35 s — every sample says *"this mode's firewall rules are missing"*. Under-reports protection, and drags Security Guard to PARTIAL. | both | fixed `fca2a92` |
| F4 | **The CLI reports only one tunnel when two are up.** `up wg08` then `up wg07 --yes`: kernel has both interfaces, default route on one and the split route on the other, but `status --json` returns a single entry and names the split tunnel as `primary`. Disconnecting one makes the other reappear. | both | open |
| F11 | **Security Guard drops the Encryption and IPv6 rows at 80×24.** At 120 cols: section words, 7 rows, `Updated …` footer. At 80×24: 5 rows, no section words, no footer — IPv6, a headline feature, is what `compact_to_fit` sheds at the most common terminal size. | macOS | fixed `6be5af0` |
| F10 | **A live managed tunnel is reported as a possible orphan.** While the TUI holds `wg08` connected, a second CLI warns `detected 1 possible orphan VPN process(es) … pid 547 (wireguard-go) … leftovers from a previous vortix crash`. It is the running session's own tunnel. | macOS | fixed `3ce196c` |
| F7 | **Connect failures name only an operation id** — `WireGuard handshake failed for operation op-…032`, `connect timed out; operation op-…034 remains recorded`. No endpoint, no reason, no next step. | both | fixed `3ce196c` |
| F3 | `--config-dir` under a world-readable parent fails closed (correct) with one opaque line **per profile** — 15 identical `persisted control state is not a private owner-controlled file` messages naming no path and no remedy. | both | fixed `ca20b67` |
| F6/F5 | A full tunnel (`AllowedIPs = 0.0.0.0/0`) rendered as `Split tunnel` under a `NO EXIT` header with `Exit IP : split-route — no exit`, while the kernel routed everything through it; the header printed the exit address under `Real:`. Primary election read a cache that **no production code ever wrote** — every caller of `feed_default_route_interface` was a test. | both | fixed `8b655e1` |
| F2 | A route-conflict refusal printed the other profile's 64-hex id instead of its name. | both | fixed `e13d3d0` |

F12 and F8 together are the sharpest argument for this file existing: 1558 automated tests
pass against a build whose kill switch does not block and whose protected state reports
itself as missing.

---

## What this file deliberately does not cover

This replaced a 230-row backlog. Rows were dropped for four reasons.

**Unreachable code.** Background mode — all of `helper/`, the daemon control host, remote
session, helper-backed executors and enrollment — is dormant behind four hard gates and is
reached from no shipped binary's `main()` (see `CLAUDE.md`). Roughly 45 rows described
enrolled VMs, helper-owned firewall/DNS/route ledgers, bootstrap trust and package identity.
None of them can be executed at all, so they were release-gating nothing. They belong with
the Background-mode plan, not in a release checklist. `vortix setup` / `vortix background`
only print a prepared preview and refuse activation; that refusal is the only shipped
behaviour, and P0-01's family of "refuses cleanly" checks is where it belongs.

**Already decided by an automated test.** Where a row's pass signal was a pure render or
parse assertion, the test already owns it:

| Dropped row | Owned by |
|---|---|
| fwmark hijack warning shown / suppressed ×3 | `ui::dashboard::connection_details::tests::fwmark_warning_*` |
| header overflow ladder and width degradation | `ui::dashboard::header::tests::narrow_strip_drops_overflow_with_plus_n`, `…::connected_header_keeps_kill_switch_visible_at_80_columns` |
| Security Guard EXPOSED / PARTIAL verdicts | `ui::dashboard::security::tests::no_tunnels_renders_exposed_with_banner_and_polish`, `…::partial_*` |
| v6 label collapse, split-route-no-exit, v6 leak alarm, `checking…` | `ui::dashboard::security::tests::partial_no_v6_connectivity_keeps_legacy_labels`, `…::partial_without_primary_keeps_split_route_no_exit_row`, `…::protected_v6_leaking_alarms_exit_ipv6_row`, `…::protected_v6_pending_renders_checking_in_real_ipv6_row` |
| real-IP cache age, load, gating | `core::real_ip_cache::tests::*`, `app::tests::real_ip_not_cached_when_scanner_has_not_ticked_yet` |
| rename validation and identity through re-sort | `app::tests::rename_rejects_*`, `…::rename_dialog_keeps_its_profile_when_background_sorting_reorders_the_list` |
| OpenVPN custody reap, force-kill, deadline escalation | `tests/tunnel_custodian.rs` |
| second-instance message | `second_tui_reports_an_actionable_already_running_message` |
| config dir private regardless of umask | `config::private_config_dir_tests::config_dir_is_created_private_regardless_of_umask` |
| WireGuard basename validation | `vortix_protocol_wireguard::tunnel::tests::managed_temp_config_rejects_a_wg_quick_invalid_basename` |
| rapid kill-switch press coalescing | `app::tests::rapid_killswitch_presses_submit_one_change_at_a_time` |

Where only the *real-kernel half* of such a row was uncovered, that half survives here —
P0-23 keeps the nft read-back but not the press coalescing, P0-29 keeps the
launch-with-VPN-up sequence but not the cache arithmetic.

**Not a test.** Environment setup ("have two WG profiles ready", "install OpenVPN 2.3"),
retired rows kept as tombstones, reserved-but-never-written rows, and one historical
diagnostic record of a failed design spike. These are prerequisites or history; they moved
to the Prerequisites table or to git.

**Not runnable by the people who run this gate.** A second OS user account, an Intel Mac, a
Windows host, a screen reader, an MDM-managed Mac, an old v0.3.x binary for downgrade
replay, a NetworkManager desktop, a CPU-contended 50× test loop. Each needs a machine or an
install nobody on the release path has. Keeping them made the list look thorough while
nothing was ever checked off. If one of those environments becomes available, the check it
needs is recoverable from git history.

**Rows that survived, compressed.** Several eight-line fault-injection paragraphs described
the same invariant from different angles — "policy barriers never publish mixed protection"
and its six restatements are one question: does a failure anywhere in the chain leave the
firewall closed and the truth honest? That is P0-21, P0-22, P0-24 and P0-27. The wording
here is shorter than the rows it replaces because a workflow nobody can finish reading is a
workflow nobody runs.

## Adding a workflow

A new workflow earns its place only if a passing automated test cannot answer it. Write the
steps as literal commands and keystrokes, and write the pass signal as something visible in a
captured frame or command output. If the pass signal needs a judgement call, it is not ready.
Add a row to the results log in the same commit.
